Para quando o e-Papirus ganhar um sininho dentro do sistema no futuro, iremos fazer uma migração adicionando:

ALTER TABLE notificacao
ADD COLUMN data_leitura DATETIME NULL;

ALTER TABLE notificacao
MODIFY COLUMN status ENUM('enviado', 'erro', 'lido');
